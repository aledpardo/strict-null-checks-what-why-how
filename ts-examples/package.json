{
  "name": "ts-examples",
  "version": "1.0.0",
  "description": "A project to make examples on how to migrate to strictNullChecks",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.build.json -w",
    "build:null-checks": "tsc -p tsconfig.null-checks.json",
    "lint": "npm run build:null-checks",
    "start": "node ./dist/index.js"
  },
  "keywords": [
    "ts",
    "null-checks",
    "stritcNullChecks"
  ],
  "author": "Alexandre Pardo <aledpviola@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.3",
    "typescript": "^5.2.2"
  }
}
