{
  "name": "ts-examples",
  "version": "1.0.0",
  "description": "A project to make examples on how to migrate to strictNullChecks",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.build.json",
    "build:null-checks": "tsc -p tsconfig.null-checks.json -w",
    "lint": "npm run build:null-checks",
    "start": "node ./dist/index.js"
  },
  "keywords": [
    "ts",
    "null-checks",
    "stritcNullChecks"
  ],
  "author": "Alexandre Pardo <aledpviola@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
